spring:
  datasource:
    url:  jdbc:mysql://mission-dev-db.cevclqs4zx1w.ap-northeast-2.rds.amazonaws.com:3306/mission_dev_db

    username: admin
    password: mission1234!
    driver-class-name: com.mysql.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    database: mysql

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernage.type: trace
